# frozen_string_literal: true
# notification-pusher 4.0.1
# Pure Rails meta-gem: the only lib file re-requires sibling Rails gems
# (notification-handler, notification-renderer, notification-settings).
# No lib/notification-pusher.rb entry point; no standalone API to exercise.
puts "notification-pusher 4.0.1"
puts "type: meta-gem (notifications-rails suite)"
puts "entry: none (lib/notifications-rails.rb re-requires Rails-only siblings)"
